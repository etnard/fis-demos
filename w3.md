# Tasks
## [Research/diagramming] Defining what a lesson is
Clarification of terms (see Demos: ubiquitous language glossary). Although from a product perspective these entities relate to each other in a nested, hierarchical way, from a technical perspective, these are all distinct entities without any meaningful connection to each other:
```
Course Outline
    Lesson Outlines
Lesson plan
Lesson
Personalized content
Quiz
```
## [Research/diagramming] Modeling the DB schema
```
lessons table
    Id (primary key, UUID)
    Timestamps (not null)
    Creation_meta (jsonb, default: {})
    Title (not null)
    Content (not null)
```
DONE? X
## [Pull request 1] Write the migration to create the lessons table
```
Include a seed record (or more)
No testing needed
```
DONE? X
## [Pull request 2] Write an endpoint that responds with an array containing the JSON representation of all lesson records
```
GET /api/lessons
Include unit testing
```
## [Pull request 3] Develop the frontend data layer
```
A service to fetch from the endpoint
A Lesson model to provide a common interface for the raw lesson data
A hook to make the above easy to use in React components
Include unit testing for each
```
## [Pull request 4] Build the index view at /lessons
```
Include unit testing where appropriate
```
## [Pull request 5] Add a new lesson demo card to the dashboard
```
The card links to /lessons
Include unit testing where appropriate
Write at least one e2e (end-to-end) test, confirming the story is satisfied
```
